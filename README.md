## Test Django

### Папку media создает Django после загрузки 1-й фотографии

> Django ищет файлы templates и static сначала в TEMPLATE_DIR и STATICFILE_DIR, а потом в папках приложений, входящих в состав проекта, в порядке установленных приложений